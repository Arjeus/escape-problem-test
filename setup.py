from setuptools import setup, find_packages

setup(
    name="escape",
    version="0.1",
    packages=find_packages(),
    install_requires=[
        "numpy",
    ],
    author="chatgpt o3",
    description="A simple Python project that uses numpy.",
)
